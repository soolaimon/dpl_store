<h1>Edit <%= @product.name %></h1>
<%= form_for(@product) do |f| %>
  <div>
   <%= f.label(:name) %>
   <%= f.text_field(:name) %>
  </div>

  <div>
    <%= f.submit('Save Product') %>
  </div>
  <%= link_to "Back to Home", products_path %>
<% end %>